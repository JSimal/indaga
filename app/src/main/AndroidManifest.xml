<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NFC" />

    <!-- Notificaciones: solo se usan para avisar de cambios en las webs que
         el propio usuario decide vigilar (sección "Vigilancia"). Se pide en
         tiempo de ejecución, no al abrir la app. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Solo permite invocar la pantalla de instalación del sistema para el
         APK que la propia app descarga desde su Release de GitHub; no
         permite instalar nada sin que el usuario confirme el diálogo final,
         eso Android no lo deja saltar nunca. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Escaneo de WiFi cercanas: Android exige ubicación para esto (los SSIDs
         pueden usarse para inferir localización). Se pide en tiempo de
         ejecución, solo al entrar en esa herramienta, con explicación previa. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Bluetooth: en Android 12+ se declara con neverForLocation porque no
         usamos los resultados para geolocalizar, solo para listar dispositivos
         cercanos; eso evita tener que pedir también permiso de ubicación ahí.
         En versiones anteriores (API 26-30) el propio sistema exige ubicación
         para el descubrimiento Bluetooth, sin alternativa. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Toolbox"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
